class ArrayTools {
	static pop(array) {
		return array[array.length];
	}

	static shift(array) {
		return array[0];
	}

	static add(array) {
		array[array.length + 1] = element;
		return null;
	}

	static arraySize(array) {
		return array.length;
	}
}
