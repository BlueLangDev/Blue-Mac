def pop(array)
{
    array.pop();
    return array;
}

def shift(array)
{
    array.remove(0);
    return array;
}

def addElement(array, element)
{
    array.add(element);
    return array;
}

def arraySize(array)
{
    return len(array);
}