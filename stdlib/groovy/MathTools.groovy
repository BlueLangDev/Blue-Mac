import java.lang.Math;

class MathTools {

def arcsine(x) {
	return Math.asin(x);
}
	
def arccos(x) {
	return Math.acos(x);
}
	
def cosine(x) {
	return Math.cos(x);
}
	
def sine(x) {
	return Math.sin(x);
}
	
def floorValue(x) {
	return Math.floor(x);
}
}